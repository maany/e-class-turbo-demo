import type { Preview } from "@storybook/react";
import "../../web/app/[locale]/globals.css"; 
import "@repo";

const preview: Preview = {
  parameters: {
    actions: { argTypesRegex: "^on[A-Z].*" },
    controls: {
      matchers: {
        color: /(background|color)$/i,
        date: /Date$/,
      },
    },
  },
};

export default preview;